<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Status Bar Overlay -->
    <View
        android:layout_width="match_parent"
        android:layout_height="24dp"
        android:background="@android:color/transparent" />

    <!-- Remote Video View (Full Screen) -->
    <org.webrtc.SurfaceViewRenderer
        android:id="@+id/remoteVideoView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true" />

    <!-- Local Video View (Picture-in-Picture) -->
    <org.webrtc.SurfaceViewRenderer
        android:id="@+id/localVideoView"
        android:layout_width="120dp"
        android:layout_height="160dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="80dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/video_frame_background" />

    <!-- Top Section - User Info (for incoming calls) -->
    <LinearLayout
        android:id="@+id/topSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/call_info_background">

        <!-- User Profile Picture -->
        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/userProfileImage"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_profile_placeholder"
            android:layout_marginBottom="16dp"
            app:civ_border_width="3dp"
            app:civ_border_color="@android:color/white" />

        <!-- User Name -->
        <TextView
            android:id="@+id/userName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="User Name"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- Call Status -->
        <TextView
            android:id="@+id/callStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Calling..."
            android:textColor="#E0E0E0"
            android:textSize="16sp"
            android:layout_marginBottom="8dp" />

        <!-- Call Duration -->
        <TextView
            android:id="@+id/callDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00"
            android:textColor="#E0E0E0"
            android:textSize="14sp"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Incoming Call Section -->
    <LinearLayout
        android:id="@+id/incomingCallSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:background="@drawable/incoming_call_background"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Incoming Video Call"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="24dp" />

        <!-- Accept/Decline Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="16dp">

            <!-- Accept Button -->
            <LinearLayout
                android:id="@+id/acceptButton"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:background="@drawable/call_accept_button"
                android:gravity="center"
                android:layout_marginEnd="32dp">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/ic_call_answer"
                    android:tint="@android:color/white" />

            </LinearLayout>

            <!-- Decline Button -->
            <LinearLayout
                android:id="@+id/declineButton"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:background="@drawable/call_decline_button"
                android:gravity="center">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/ic_call_end"
                    android:tint="@android:color/white" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

    <!-- Bottom Section - Call Controls -->
    <LinearLayout
        android:id="@+id/bottomSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:background="@drawable/call_controls_background"
        android:visibility="visible">

        <!-- Call Duration (when connected) -->
        <TextView
            android:id="@+id/callDurationConnected"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:layout_marginBottom="24dp"
            android:visibility="gone" />

        <!-- Call Control Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="16dp">

            <!-- Mute Button -->
            <LinearLayout
                android:id="@+id/muteButton"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@drawable/call_button_normal"
                android:gravity="center"
                android:layout_marginEnd="16dp">

                <ImageView
                    android:id="@+id/muteIcon"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_mic"
                    android:tint="@android:color/white" />

            </LinearLayout>

            <!-- Camera Switch Button -->
            <LinearLayout
                android:id="@+id/cameraSwitchButton"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@drawable/call_button_normal"
                android:gravity="center"
                android:layout_marginEnd="16dp">

                <ImageView
                    android:id="@+id/cameraSwitchIcon"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_camera_switch"
                    android:tint="@android:color/white" />

            </LinearLayout>

            <!-- Speaker Button -->
            <LinearLayout
                android:id="@+id/speakerButton"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@drawable/call_button_normal"
                android:gravity="center"
                android:layout_marginEnd="16dp">

                <ImageView
                    android:id="@+id/speakerIcon"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_volume_down"
                    android:tint="@android:color/white" />

            </LinearLayout>

            <!-- End Call Button -->
            <LinearLayout
                android:id="@+id/endCallButton"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@drawable/call_end_button"
                android:gravity="center">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_call_end"
                    android:tint="@android:color/white" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</RelativeLayout>
